deps            # directory to hold cloned repos that are needed for firmware app build
docs            # directory for documents
firmware        # directory to store nfp firmware applications, build, and deps
  apps          # directory for nfp firmware applications
    vrouter             
  build         # directory used for application building (empty dir in the repo)
  lib
  nffw          # directory to store compiled nfp application firmware
kernel          # directory for kernel driver related code
  nfp_vrouter
scripts         # directory for helpful executable scripts
src             # directory to store host applications (possibly named user or host)
include         # directory to store common source files

